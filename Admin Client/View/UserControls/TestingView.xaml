<Window x:Class="Admin_Client.View.UserControls.TestingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Admin_Client.View.UserControls"
        mc:Ignorable="d"
        Title="TestingView" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox x:Name="ListResult" ItemsSource="{Binding Groups}" Grid.Row="0" Grid.Column="1"></ListBox>
        <TextBlock x:Name="TextResult" Grid.Row="1" Grid.Column="1" ></TextBlock>
        <Button x:Name="Confirm" Grid.Row="0" Grid.Column="0" Content="Confirm" Click="Confirm_Click"></Button>
        
    </Grid>
</Window>
